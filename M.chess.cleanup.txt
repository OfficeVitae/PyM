; chess.cleanup() takes care of removing any remote player comm link
setdebug(debugnow) ; reset debug
if(and(remoteplayerindex>0,brchannel>=0),(out("cleanup"),brend(brchannel))) ; close communication channel
